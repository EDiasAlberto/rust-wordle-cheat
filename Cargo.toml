[package]
name = "rust-wordle"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
text_io = { version = "0.1.13", features = [] }
